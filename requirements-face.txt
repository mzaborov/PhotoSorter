# Отдельное окружение (.venv-face, Python 3.12) для распознавания лиц.
#
# Причина: под Python 3.14 на Windows пока нет готовых wheels для onnxruntime/numpy/opencv,
# поэтому для ML-части используем Python 3.12.

numpy==2.2.6
opencv-python==4.12.0.88
onnxruntime==1.23.2
pillow==12.1.0
yadisk==3.4.0
python-dotenv==1.2.1
requests==2.32.5

