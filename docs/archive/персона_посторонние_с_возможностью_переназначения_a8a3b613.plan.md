---
name: Персона "Посторонние" с возможностью переназначения
overview: Добавляю поддержку специальной персоны "Посторонние" для кластеров с неинтересными лицами. Такие кластеры показываются внизу списка с визуальным отличием. Выпадашка в карточке кластера позволяет переназначать персону, включая снятие назначения.
todos:
  - id: backend-constant
    content: Добавить константу IGNORED_PERSON_NAME = 'Посторонние' в face_clusters.py
    status: completed
  - id: backend-sorting
    content: "Изменить сортировку в api_face_clusters_list: персоны \"Посторонние\" в конце"
    status: completed
    dependencies:
      - backend-constant
  - id: backend-unassign
    content: Добавить поддержку снятия назначения (person_id=None в assign_cluster_to_person)
    status: completed
    dependencies:
      - backend-constant
  - id: frontend-dropdown-current
    content: "Обновить выпадашку: показывать текущую персону и опцию 'Снять назначение'"
    status: completed
    dependencies:
      - backend-unassign
  - id: frontend-separation
    content: "Разделить рендеринг: обычные персоны сверху, \"Посторонние\" внизу"
    status: completed
    dependencies:
      - backend-sorting
  - id: frontend-visual
    content: Добавить визуальное отличие для группы "Посторонние" (серый цвет, иконка)
    status: completed
    dependencies:
      - frontend-separation
  - id: css-styles
    content: Добавить CSS стили для .person-group.ignored
    status: completed
    dependencies:
      - frontend-visual
---

# План: Персона "Посторонние" с возможностью переназначения

## Цель

Создать специальную персону "Посторонние" для кластеров с неинтересными лицами. Такие кластеры показываются внизу списка с визуальным отличием. Выпадашка в карточке кластера позволяет переназначать персону, включая снятие назначения и возврат из "Посторонние".

## Изменения

### 1. Backend: Определение специальной персоны

- **Файл**: `backend/web_api/routers/face_clusters.py`
- Добавить константу `IGNORED_PERSON_NAME = "Посторонние"`
- В функции `api_persons_list` помечать персону "Посторонние" специальным флагом `is_ignored: true`
- В SQL-запросах `api_face_clusters_list` сортировать персон так, чтобы "Посторонние" были в конце:
  ```sql
  ORDER BY 
    CASE WHEN p2.name = 'Посторонние' THEN 1 ELSE 0 END,
    MAX(p.name) ASC,
    faces_count DESC
  ```


### 2. Backend: API для снятия назначения

- **Файл**: `backend/web_api/routers/face_clusters.py`
- Добавить endpoint `DELETE /api/face-clusters/{cluster_id}/assign-person` для снятия назначения
- Или использовать существующий `POST /api/face-clusters/{cluster_id}/assign-person` с `person_id: null`
- В функции `assign_cluster_to_person` обработать случай `person_id=None` - удалить все назначения без создания новых

### 3. Frontend: Выпадашка с текущим значением и опцией "Снять назначение"

- **Файл**: `backend/web_api/templates/face_clusters.html`
- В функции `createClusterCard()`:
  - Если кластер назначен персоне - показывать выпадашку с текущей персоной выбранной
  - Добавить опцию "Снять назначение" (value="") в начало выпадашки
  - При выборе другой персоны - переназначать
  - При выборе "Снять назначение" - удалять назначение (отправлять `person_id: null` или вызывать DELETE endpoint)

### 4. Frontend: Визуальное отличие и сортировка

- **Файл**: `backend/web_api/templates/face_clusters.html`
- В функции `renderClusters()`:
  - Разделить персон на обычные и "Посторонние"
  - Сначала рендерить обычных персон
  - Затем рендерить "Посторонние" внизу
  - Применить визуальное отличие для группы "Посторонние":
    - Серый цвет для заголовка группы `#f3f4f6`
    - Иконка "🚫" вместо аватара
    - Приглушенный стиль карточек кластеров

### 5. Стили CSS

- **Файл**: `backend/web_api/templates/face_clusters.html` (секция `<style>`)
- Добавить класс `.person-group.ignored`:
  ```css
  .person-group.ignored .person-group-header {
    background: #f3f4f6;
    opacity: 0.7;
  }
  .person-group.ignored .person-name {
    color: #6b7280;
  }
  .person-group.ignored .card {
    opacity: 0.8;
    border-color: #e5e7eb;
  }
  ```


### 6. Создание персоны "Посторонние"

- **Вариант**: Автоматически создавать при первом использовании
  - В `api_persons_create` или отдельной функции проверять наличие персоны "Посторонние"
  - Если нет - создать автоматически с `mode='ignored'` или специальным флагом

## Детали реализации

### Выпадашка в карточке кластера

```javascript
// Если кластер назначен
<select onchange="assignPersonFromDropdown(clusterId, this.value)">
  <option value="">Снять назначение</option>
  <option value="${currentPersonId}" selected>${currentPersonName}</option>
  <!-- остальные персоны -->
</select>

// Если кластер не назначен
<select onchange="assignPersonFromDropdown(clusterId, this.value)">
  <option value="">Назначить персону...</option>
  <!-- все персоны -->
</select>
```

### API для снятия назначения

```python
# В assign_cluster_to_person
if person_id is None:
    # Удаляем все назначения
    cur.execute("DELETE FROM face_labels WHERE cluster_id = ?", (cluster_id,))
    conn.commit()
    return
```

## Порядок выполнения

1. Добавить константу и логику определения "Посторонние" в backend
2. Изменить сортировку в SQL-запросе списка кластеров
3. Добавить поддержку снятия назначения (person_id=None)
4. Обновить выпадашку: показывать текущую персону и опцию "Снять назначение"
5. Обновить frontend для разделения и визуального отличия
6. Добавить CSS стили
7. Протестировать создание, назначение и переназначение кластеров