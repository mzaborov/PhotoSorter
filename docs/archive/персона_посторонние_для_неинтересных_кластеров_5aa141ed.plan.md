---
name: Персона "Посторонние" для неинтересных кластеров
overview: Добавляю поддержку специальной персоны "Посторонние" для кластеров с неинтересными лицами. Такие кластеры будут показываться внизу списка с визуальным отличием (серый цвет, иконка).
todos:
  - id: backend-constant
    content: Добавить константу IGNORED_PERSON_NAME = 'Посторонние' в face_clusters.py
    status: pending
  - id: backend-sorting
    content: "Изменить сортировку в api_face_clusters_list: персоны \"Посторонние\" в конце"
    status: pending
    dependencies:
      - backend-constant
  - id: frontend-separation
    content: "Разделить рендеринг: обычные персоны сверху, \"Посторонние\" внизу"
    status: pending
    dependencies:
      - backend-sorting
  - id: frontend-visual
    content: Добавить визуальное отличие для группы "Посторонние" (серый цвет, иконка)
    status: pending
    dependencies:
      - frontend-separation
  - id: css-styles
    content: Добавить CSS стили для .person-group.ignored
    status: pending
    dependencies:
      - frontend-visual
---

# План: Персона "Посторонние" для неинтересных кластеров

## Цель
Создать специальную персону "Посторонние" для кластеров с неинтересными лицами. Такие кластеры будут показываться внизу списка с визуальным отличием.

## Изменения

### 1. Backend: Определение специальной персоны
- **Файл**: `backend/web_api/routers/face_clusters.py`
- Добавить константу `IGNORED_PERSON_NAME = "Посторонние"` или использовать флаг `mode` в таблице `persons`
- В функции `api_persons_list` помечать персону "Посторонние" специальным флагом (например, `is_ignored: true`)
- В SQL-запросах `api_face_clusters_list` сортировать персон так, чтобы "Посторонние" были в конце

### 2. Frontend: Визуальное отличие и сортировка
- **Файл**: `backend/web_api/templates/face_clusters.html`
- В функции `renderClusters()`:
  - Разделить персон на обычные и "Посторонние"
  - Сначала рендерить обычных персон
  - Затем рендерить "Посторонние" внизу
  - Применить визуальное отличие для группы "Посторонние":
    - Серый цвет для заголовка группы
    - Иконка "🚫" или "👤❌" вместо аватара
    - Приглушенный стиль карточек кластеров

### 3. Стили CSS
- **Файл**: `backend/web_api/templates/face_clusters.html` (секция `<style>`)
- Добавить класс `.person-group.ignored`:
  - Приглушенные цвета
  - Серый фон для заголовка
  - Возможно, меньший размер шрифта

### 4. Создание персоны "Посторонние"
- **Вариант A**: Автоматически создавать при первом использовании
  - В `api_persons_create` или отдельной функции проверять наличие персоны "Посторонние"
  - Если нет - создать автоматически
- **Вариант B**: Создать вручную через UI (пользователь сам создаст)

## Детали реализации

### Сортировка в SQL
```sql
ORDER BY 
  CASE WHEN p.name = 'Посторонние' THEN 1 ELSE 0 END,
  MAX(p.name) ASC,
  faces_count DESC
```

### Визуальное отличие
- Заголовок группы: серый фон `#f3f4f6`, приглушенный текст
- Иконка вместо аватара: `🚫` или `👤❌`
- Карточки кластеров: легкая прозрачность или серый border

## Порядок выполнения
1. Добавить константу и логику определения "Посторонние" в backend
2. Изменить сортировку в SQL-запросе списка кластеров
3. Обновить frontend для разделения и визуального отличия
4. Добавить CSS стили
5. Протестировать создание и назначение кластеров "Посторонним"